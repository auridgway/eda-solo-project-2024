const gamesReducer = (state = {}, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  // user will be on the redux state at:
  // state.user
  export default gamesReducer;